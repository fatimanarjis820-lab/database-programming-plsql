name: Students Table Creation

on:
  push:
    branches: ["main", "sql-hands-on"]

jobs:
  runs-sql:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: testdb
        # No port mapping needed for internal CI access

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install MySQL Client
        run: |
          sudo apt-get update
          sudo apt-get install mysql-client -y

      - name: Wait for MySQL to Start
        run: |
          echo "Waiting for MySQL to start..."
          for i in {1..60}; do
            if mysql -hmysql -uroot -proot -e "SELECT 1;" >/dev/null 2>&1; then
              echo "MySQL is ready!"
              break
            fi
            echo "Waiting... ($i)"
            sleep 2
          done

      - name: Run SQL File
        run: |
          mysql -hmysql -uroot -proot testdb < SQL/students.sql
